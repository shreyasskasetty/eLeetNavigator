import GoogleLoginButton from '../components/GoogleLoginButton'; // Ensure this component is correctly exported from its module
function LoginPage() {
  return (
    <div>
        <GoogleLoginButton />
    </div>
  );
}

export default LoginPage;
